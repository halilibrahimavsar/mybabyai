app:
  debug: false
  language: tr
  name: MyBabyAI
  version: 1.0.0
gui:
  font_family: Segoe UI
  font_size: 11
  sidebar_width: 300
  theme: dark
  window_size:
  - 1200
  - 800
inference:
  max_new_tokens: 2048
  temperature: 0.7
  top_k: 50
  top_p: 0.95
memory:
  chunk_overlap: 50
  chunk_size: 512
  embedding_model: sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
  max_history: 100
  vector_db_path: data/chroma
model:
  codemind:
    checkpoint_dirs:
    - checkpoints
    - checkpoints_instruct
    min_compatibility_ratio: 0.6
  default_model: CodeMind-125M
  device: cuda
  load_in_4bit: false
  lora:
    lora_alpha: 32
    lora_dropout: 0.05
    r: 16
    target_modules:
    - q_proj
    - v_proj
    - k_proj
    - o_proj
  max_memory: 10GB
  model_path: models/
  name: CodeMind-125M
training:
  per_device_train_batch_size: 16
  gradient_accumulation_steps: 8
  learning_rate: 0.00015
  max_length: 1024
  gradient_checkpointing: true
  num_train_epochs: 3
  optim: paged_adamw_8bit
  output_dir: models/fine_tuned
  logging_steps: 10
  save_steps: 100
  warmup_steps: 100
  pack_sequences: true
  torch_compile: false
  qlora:
    bnb_4bit_compute_dtype: float16
    bnb_4bit_quant_type: nf4
    use_qlora: true
